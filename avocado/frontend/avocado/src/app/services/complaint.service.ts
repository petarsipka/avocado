import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ComplaintService {
  // TODO: Connect to backend
  // Endpoints: GET /api/complaints, POST /api/complaints, etc.
}