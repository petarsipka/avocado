import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ReasoningService {
  // TODO: Connect to backend
  // Endpoints: POST /api/reasoning/classify, /qualify, /rights, /advice, /backward-chain
}